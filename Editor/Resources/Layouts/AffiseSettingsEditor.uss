.settings-create {
    height: 40px;
}

.settings-label {
    padding-top: 2px;
    padding-left: 9px;
    font-size: 19px;
    -unity-font-style: bold;
}

.panel {
    border-radius: 2px;
    background-color: rgba(0, 0, 0, 0.1);
}
